# Walkthrough

The locked walkthrough script and its command sequence live in the
`meridian-goods` repository, checkpointed as a series of `walkthrough/*`
tags — one tag per beat, each reproducible from a clean clone.

This directory will hold the script itself once it is locked.
